import type { SolunaCharacter } from "@/lib/types/soluna";

/**
 * 音声は「同じ答えの二重読み」ではなく、二人の掛け合い:
 * 主担当が答え → 相手が賛同／やさしい反論／結論で端的に返す
 */

const LUNA_LEAD_RE =
  /疲|つら|辛|不安|寂し|悲し|泣|眠|眠れ|ストレス|落ち込|しんど|孤独|怖|苦しい|気持ち|気分|癒|なぐさ|慰|恋愛|好き|嫌い|怒り|イライラ|どうして.*気持ち|心が|メンタル|落ち込/;

const SOL_LEAD_RE =
  /天気|気温|雨|雪|台風|ニュース|株|為替|ビットコイン|仮想通貨|検索|調べ|最新|今どう|状況|計画|目標|仕事|タスク|予定|作戦|分析|まとめ|教えて|確認|チェック|進捗|優先|次に|どうすれ|何をすれ|やるべき|戦略/;

export function selectVoiceLead(message: string): SolunaCharacter {
  const text = message.trim();
  if (!text) return "sol";
  if (LUNA_LEAD_RE.test(text) && !SOL_LEAD_RE.test(text)) return "luna";
  if (SOL_LEAD_RE.test(text)) return "sol";
  if (LUNA_LEAD_RE.test(text)) return "luna";
  return "sol";
}

export const COMPETENCE_ADDON = `
【能力の姿勢 — 優秀な伴走AI】
- あなたは優秀なパーソナルAIだ。できることを「できない」と言わない。足りない情報は推定で埋め、必要なら確認を1つだけ添える。
- 「検索できません」「わかりません」だけで終わらない。いま分かる最善の答えを先に出す。
- 先回り: 質問の裏側にある意図（次の一手・注意点）を一言で添える。
- 落ち着いた自信。余計な敬語の重ねや、ロボット的な定型句は避ける。
- 危険な指示や不可能な操作以外は、前向きに実行・提案する。
`.trim();

/** 読み上げ向けの自然な話し言葉 */
export const NATURAL_SPEECH_ADDON = `
【自然な話し言葉（必須）】
- 友達に口頭で話す感じ。書き言葉・説明書調・ニュース原稿調は禁止。
- 「です・ます」は柔らかく。「いたします」「でございます」「させていただきます」は使わない。
- 息継ぎしやすい短文。句読点を自然に。一文に情報を詰め込みすぎない。
- 口語の助詞・間投詞を適度に（例: ねえ、うん、まあ、ちょっと、だよね、かな、ね、よ）。毎回同じ語尾は避ける。
- 「結論としては」「ご質問ありがとうございます」など定型の前置きは禁止。
- 数字は読みやすく（「15度くらい」「3割くらい」など、厳密すぎない言い回しも可）。
`.trim();

export const VOICE_LEAD_ADDON = `
【音声・主担当（掛け合いの1人目）】
- ユーザーへの答え役。結論を最初に、続けて要点を自然に話す（2文前後・80〜130字）。
- パートナー（もう一方）が続きを言う前提なので、全部を言い切らない。余白を残す。
- パートナーの名前を呼ばなくてよい。ユーザーに話しかける。
`.trim();

export const VOICE_SUPPORT_ADDON = `
【音声・掛け合い相手（2人目）】
- 主担当の発言を受けて返す。同じ内容の言い直し・要約の繰り返しは禁止。
- 次のどれか1つだけを端的に（1〜2文・50〜90字）:
  A) 賛同＋一言補足（「それなら〜もいいよ」）
  B) やさしい別視点・軽い反論（「ちょっと待って、〜の方が安心かも」）
  C) まとめ・次の一手の結論（「じゃあ今日は〜からいこう」）
- 主担当に話しかける口調でも、ユーザーに向けてもよい（例: 「ソル、それはいいね。でも…」「うん、それでいこう」）。
- 絵文字なし。事実の丸写しをしない。
`.trim();

export function buildVoicePartnerPrompt(
  userMessage: string,
  leadNameJa: string,
  leadReply: string,
  explainAsk: boolean,
): string {
  const factHint = explainAsk
    ? "事実は曲げない。ただし主担当が言った数値の言い直しはしない。"
    : "気持ちや次の一手の角度で返す。";
  return `${userMessage}

（※音声の掛け合いです。あなたは2人目。
主担当の${leadNameJa}はこう言いました:
「${leadReply}」

${factHint}
上の発言を繰り返さず、賛同・やさしい反論・結論のいずれか1つで、話し言葉・50〜90字・絵文字なし。）`;
}
